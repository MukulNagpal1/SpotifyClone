import 'package:flutter/material.dart';

class Gems extends StatelessWidget {
  const Gems({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("This is gems page"),
      ),
    );
  }
}
